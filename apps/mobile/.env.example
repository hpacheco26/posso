# Selects which Firebase project + bundle id the app targets.
#   development -> posso-dev  (pt.posso.app.dev)
#   production  -> posso-prod (pt.posso.app)
# EAS build profiles set this automatically (see eas.json); for local dev the
# default is "development".
APP_ENV=development
