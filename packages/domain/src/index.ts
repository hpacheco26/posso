/**
 * @posso/domain — pure, framework-free domain logic for Posso?.
 *
 * RULES:
 * - No imports from `firebase`, `react`, `react-native`, or `expo` may appear
 *   anywhere in this package. It must stay a pure, deterministic, unit-testable core.
 * - This is where the Food Safety Engine, recommendation scoring, nutrition math,
 *   shopping aggregation, and pregnancy-week calculations will live.
 */

export const DOMAIN_PACKAGE = "@posso/domain";
