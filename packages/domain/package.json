{
  "name": "@posso/domain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pure, framework-free domain logic for Posso? (food-safety engine, recommender, nutrition, pregnancy math). No Firebase, React, or Expo.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
