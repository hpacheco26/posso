auto-install-peers=true
strict-peer-dependencies=false
# React Native / Metro do not reliably resolve pnpm's isolated (symlinked)
# node_modules, so use a flat, hoisted layout for the whole workspace.
node-linker=hoisted
